# Project Memory

Project-specific reconnect memory lives here. Use one folder per project only after the state is verified from authoritative sources.

Project memory is not reusable knowledge by default. When a project observation appears useful elsewhere, create or strengthen a canonical entry under `knowledge/` and link evidence rather than copying the same prose across projects.

Use `templates/project-memory.md`.
